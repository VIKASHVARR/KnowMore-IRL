<svg width="512" height="512" viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg">
  <!-- Rounded white background -->
  <rect width="512" height="512" rx="100" fill="#FFFFFF"/>
  
  <!-- Arrow pointing upper-left (composed of two rectangles forming an L shape rotated) -->
  <g transform="translate(256, 256) rotate(-135)">
    <!-- Vertical part of arrow -->
    <rect x="-30" y="-140" width="60" height="180" fill="#003D82" rx="10"/>
    <!-- Horizontal part of arrow -->
    <rect x="-140" y="-30" width="180" height="60" fill="#003D82" rx="10"/>
    <!-- Arrow tip triangle -->
    <polygon points="0,-160 -40,-100 40,-100" fill="#003D82"/>
  </g>
  
  <!-- Orange dot at the top -->
  <circle cx="180" cy="180" r="35" fill="#FF6B00"/>
</svg>
